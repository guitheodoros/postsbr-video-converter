{
  "name": "postsbr-video-converter",
  "version": "1.0.0",
  "private": true,
  "description": "Small server that converts a WebM recording to an Instagram-ready MP4 using native ffmpeg.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ffmpeg-static": "^5.2.0",
    "multer": "^2.0.0"
  }
}
